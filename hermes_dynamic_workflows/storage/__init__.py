"""Persistent workflow storage.

Run records, persisted scripts, named-workflow lookup, and the file-backed
control request/response queue. Depends on ``core`` (and ``host`` for the Hermes
home directory); imports nothing upward.
"""
