"""Core leaf layer: shared types, errors, config, schema, and text helpers.

Modules here have no internal dependencies on other workflow subsystems, so any
layer may import from ``core`` without risking an import cycle.
"""
