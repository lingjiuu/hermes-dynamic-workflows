"""Pure text rendering of workflow run snapshots.

Renders plain snapshot dicts to text/markdown for slash commands and exports.
Depends only on ``core``; imports nothing upward, so no layer cycles through it.
"""
